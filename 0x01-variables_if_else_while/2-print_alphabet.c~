#include <stdio.h>

/**
 * main - Entry point
 * c - Character to be printed
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; ++c)
		putchar(c'\n');
	/* putchar('\n'); */

	return (0);
}
